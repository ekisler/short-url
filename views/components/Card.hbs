<article class="card mb-2">
            <div class="card-body">
                <p>{{iten.origin}}/</p>
                <p>{{iten.shortURL}}</p>
                <a href="/eliminar/{{iten._id}}" class="btn btn-danger">Eliminar</a>
                <a href="/editar/{{iten._id}}" class="btn btn-warning">Editar</a>
                <button data-short="{{iten.shortURL}}" href="#" class="btn btn-info">Copiar</button>
            </div>
        </article> 